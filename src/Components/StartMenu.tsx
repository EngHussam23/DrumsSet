const StartMenu = () => {
  return (
    <div className="StartMenu">
      <button className="CTA">
        <p>Start Game</p>
      </button>
      <button className="CTA">
        <p>Settings</p>
      </button>
    </div>
  );
};

export default StartMenu;
